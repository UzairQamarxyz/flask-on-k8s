apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-flask-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}-flask
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-flask
  ports:
    - name: http
      port: 80
      targetPort: 5000
